'use client';

// 🍽️ منيو المكان من لوحة الإدارة — /admin/venues/menu
// نفس محرّر كونسول المكان حرفيّاً (مصدرٌ واحد): الأصناف المفردة والباقات
// والأسعار وحصّة النادي. المكان يُختار من منتقي الـlayout.
import VenueMenuPage from '../../../venue/menu/page';

export default function AdminVenueMenuPage() {
  return <VenueMenuPage />;
}
