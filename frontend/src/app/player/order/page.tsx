'use client';

// ══════════════════════════════════════════════════════
// 🍽️ اطلب من المكان — /player/order
// صفحةٌ كاملة لمن يصل من بطاقة الرئيسيّة أو من إشعار حالة الطلب.
// المنطق والواجهة كلّها في OrderPanel — وهو نفسه ما يُعرض ورقةً منسدلة
// داخل شاشة اللعبة، فلا تتفرّع تجربتان.
// ══════════════════════════════════════════════════════

import OrderPanel from '@/components/OrderPanel';

export default function PlayerOrderPage() {
  return <OrderPanel />;
}
